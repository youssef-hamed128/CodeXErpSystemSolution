@model CodeXErpSystem.BLL.ViewModels.Reports.SalesByCustomerViewModel
@{ Layout = "_Layout"; }

    <nav class="breadcrumb">
        <div class="breadcrumb-item"><a href="~/Home/Index"><i class="fas fa-home"></i> الرئيسية</a></div>
        <div class="breadcrumb-item"><a href="~/Reports/Index">التقارير</a></div>
        <div class="breadcrumb-item active">المبيعات حسب العميل</div>
    </nav>
    <div class="page-header">
        <div class="page-header-left">
            <h1 class="page-title"><i class="fas fa-chart-line" style="color:var(--primary);"></i> تقرير المبيعات حسب العميل</h1>
        </div>
        <div class="page-header-right">
            <button class="btn btn-outline-secondary btn-sm" onclick="exportToExcel('reportTable')"><i class="fas fa-file-excel"></i> تصدير Excel</button>
            <button class="btn btn-primary" onclick="window.print()"><i class="fas fa-print"></i> طباعة</button>
        </div>
    </div>

    <form method="get" action="~/Reports/SalesByCustomer">
        <div class="card" style="padding:var(--spacing-4); margin-bottom:var(--spacing-5);">
            <div class="form-grid-3">
                <div class="form-group">
                    <label class="form-label">من تاريخ</label>
                    <input type="date" name="startDate" class="form-control" value="@Model.StartDate.ToString("yyyy-MM-dd")">
                </div>
                <div class="form-group">
                    <label class="form-label">إلى تاريخ</label>
                    <input type="date" name="endDate" class="form-control" value="@Model.EndDate.ToString("yyyy-MM-dd")">
                </div>
                <div class="form-group" style="display:flex;align-items:flex-end;">
                    <button type="submit" class="btn btn-primary" style="width:200px;"><i class="fas fa-filter"></i> تحديث التقرير</button>
                </div>
            </div>
        </div>
    </form>

    <div class="data-table-wrapper">
        <div class="table-responsive">
            <table class="data-table" id="reportTable">
                <thead>
                    <tr>
                        <th>العميل</th>
                        <th>عدد الفواتير</th>
                        <th>إجمالي المبيعات (ج.م)</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var c in Model.Customers)
                    {
                        <tr>
                            <td>@c.CustomerName</td>
                            <td class="numeric">@c.InvoicesCount</td>
                            <td class="numeric" style="font-weight:600;color:var(--primary);">@c.TotalSales.ToString("N2")</td>
                        </tr>
                    }
                    @if(!Model.Customers.Any())
                    {
                        <tr><td colspan="3" class="center">لا توجد مبيعات في هذه الفترة.</td></tr>
                    }
                    <tr style="background:var(--bg-active);font-weight:800;font-size:16px;">
                        <td colspan="2">الإجمالي</td>
                        <td class="numeric" style="color:var(--primary);">@Model.Customers.Sum(c => c.TotalSales).ToString("N2")</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
